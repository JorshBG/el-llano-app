{
    "private": true,
    "scripts": {
        "compose": "composer install && composer dumpautoload && php artisan migrate && php artisan migrate:fresh --seed",
        "load-pnpm": "pnpm import && pnpm install",
        "dev": "vite",
        "build": "vite build"
    },
    "devDependencies": {
        "laravel-vite-plugin": "^0.7.4",
        "vite": "^4.2.1"
    },
    "dependencies": {
        "axios": "^1.3.5",
        "datatables.net": "^1.13.4",
        "dotenv": "^16.0.3"
    }
}
